{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "outDir": "dist",
    "noEmit": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts"],
  "//": "forge-mock/forge-real are tsx-run e2e drivers that import the sibling game package's .ts sources directly; NodeNext tsc can't resolve those cross-package paths, and they are type-checked by running under tsx, so they are excluded from this compile-check.",
  "exclude": ["test/forge-mock.ts", "test/forge-real.ts"]
}
